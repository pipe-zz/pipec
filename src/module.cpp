#include <iostream>

#include "../include/pipe/module.h"

pipe::module pipe::module::create_from_file(std::string path) {
	
	
}

pipe::module pipe::module::create_from_source(std::string src) {
	
}
